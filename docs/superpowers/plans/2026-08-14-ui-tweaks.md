# UI tweaks — implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Remove the IORI graph, fix the video while only the graphs scroll, list the directory's files in the dropdown, and add a copy-path button.

**Architecture:** Small backend trim + frontend markup/CSS/JS changes; no data-model change.

**Tech Stack:** Python stdlib backend, vanilla JS/HTML/CSS.

## Global Constraints

- Vanilla JS/HTML/CSS only; no framework/CDN; no new deps.
- No JS test harness — manual acceptance.
- `CORI` (camera orientation) stays; only `IORI` is removed.

---

## File structure

- Modify `gopro_accel/server.py` — drop IORI from `/api/accel`.
- Modify `gopro_accel/static/index.html` — copy button, `#graphs` scroll wrapper, remove IORI block.
- Modify `gopro_accel/static/style.css` — app-shell flex layout.
- Modify `gopro_accel/static/app.js` — remove IORI graph, dropdown-from-directory, copy handler.

---

## Task 1: Drop IORI from the backend

**Files:**
- Modify: `gopro_accel/server.py`

- [ ] **Step 1: Remove the IORI parse and response keys**

In `gopro_accel/server.py` `_accel`, replace this block:

```python
            cori = parse_stream(blob, times, "CORI", dur)
            iori = parse_stream(blob, times, "IORI", dur)

            def comp(s, i):
                return s.comps[i] if i < len(s.comps) else []

            return self._json({
                "t": accel.t, "ax": comp(accel, 0), "ay": comp(accel, 1), "az": comp(accel, 2), "amag": accel.mag,
                "gt": gyro.t, "gx": comp(gyro, 0), "gy": comp(gyro, 1), "gz": comp(gyro, 2), "gmag": gyro.mag,
                "gvt": grav.t, "gvx": comp(grav, 0), "gvy": comp(grav, 1), "gvz": comp(grav, 2), "gvmag": grav.mag,
                "cot": cori.t, "cow": comp(cori, 0), "cox": comp(cori, 1), "coy": comp(cori, 2), "coz": comp(cori, 3),
                "iot": iori.t, "iow": comp(iori, 0), "iox": comp(iori, 1), "ioy": comp(iori, 2), "ioz": comp(iori, 3),
                "warnings": accel.warnings + gyro.warnings + grav.warnings + cori.warnings + iori.warnings,
                "fps": probe_fps(safe),
            })
```

with:

```python
            cori = parse_stream(blob, times, "CORI", dur)

            def comp(s, i):
                return s.comps[i] if i < len(s.comps) else []

            return self._json({
                "t": accel.t, "ax": comp(accel, 0), "ay": comp(accel, 1), "az": comp(accel, 2), "amag": accel.mag,
                "gt": gyro.t, "gx": comp(gyro, 0), "gy": comp(gyro, 1), "gz": comp(gyro, 2), "gmag": gyro.mag,
                "gvt": grav.t, "gvx": comp(grav, 0), "gvy": comp(grav, 1), "gvz": comp(grav, 2), "gvmag": grav.mag,
                "cot": cori.t, "cow": comp(cori, 0), "cox": comp(cori, 1), "coy": comp(cori, 2), "coz": comp(cori, 3),
                "warnings": accel.warnings + gyro.warnings + grav.warnings + cori.warnings,
                "fps": probe_fps(safe),
            })
```

- [ ] **Step 2: Run the suite**

Run: `python -m pytest -q`
Expected: PASS (unchanged — no test asserts on the IORI keys).

- [ ] **Step 3: Commit**

```bash
git add gopro_accel/server.py
git commit -m "feat: drop IORI from /api/accel"
```

---

## Task 2: Markup and layout — copy button, fixed video, scroll graphs, remove IORI

**Files:**
- Modify: `gopro_accel/static/index.html`
- Modify: `gopro_accel/static/style.css`

- [ ] **Step 1: Add the copy-path button to the file bar**

In `gopro_accel/static/index.html`, replace:

```html
  <div class="bar">
    <label for="filesel">Файл</label>
    <select id="filesel"></select>
    <button id="browse">Обзор</button>
  </div>
```

with:

```html
  <div class="bar">
    <label for="filesel">Файл</label>
    <select id="filesel"></select>
    <button id="browse">Обзор</button>
    <button id="copypath" type="button">Копировать путь</button>
  </div>
```

- [ ] **Step 2: Wrap the graphs in a scroll container and remove the IORI block**

Replace the whole region from `<canvas id="scrub" height="240"></canvas>` through the image-orientation label row (the block ending `<div class="controls"><span class="glabel">Ориентация изображения (кватернион)</span></div>`):

```html
  <canvas id="scrub" height="240"></canvas>

  <div id="panbar"><div id="panthumb"></div></div>

  <div id="legend"></div>

  <div class="controls">
    <span id="tlabel" class="mono">00:00 / 00:00</span>
    <span class="spacer"></span>
    <div id="zoom">
      <button id="zoomout" type="button" aria-label="Уменьшить масштаб">−</button>
      <button id="zoomin" type="button" aria-label="Увеличить масштаб">+</button>
    </div>
    <button id="zoomreset" type="button" aria-label="Сбросить масштаб">⤢</button>
    <div id="mode">
      <button data-m="mag" class="mbtn on">|a|</button>
      <button data-m="xyz" class="mbtn">X / Y / Z</button>
    </div>
  </div>
  <div id="warnings"></div>

  <canvas id="scrub2" height="240"></canvas>

  <div id="legend2"></div>

  <div class="controls">
    <span class="glabel">Гироскоп</span>
    <span class="spacer"></span>
    <div id="gmode">
      <button data-m="mag" class="gbtn on">|ω|</button>
      <button data-m="xyz" class="gbtn">X / Y / Z</button>
    </div>
  </div>

  <canvas id="scrub3" height="240"></canvas>
  <div id="legend3"></div>
  <div class="controls">
    <span class="glabel">Гравитация</span>
    <span class="spacer"></span>
    <div id="rmode">
      <button data-m="mag" class="rbtn">|g|</button>
      <button data-m="xyz" class="rbtn on">X / Y / Z</button>
    </div>
  </div>

  <canvas id="scrub4" height="240"></canvas>
  <div id="legend4"></div>
  <div class="controls"><span class="glabel">Ориентация камеры (кватернион)</span></div>

  <canvas id="scrub5" height="240"></canvas>
  <div id="legend5"></div>
  <div class="controls"><span class="glabel">Ориентация изображения (кватернион)</span></div>
```

with (wrapped in `#graphs`, IORI block removed):

```html
  <div id="graphs">
  <canvas id="scrub" height="240"></canvas>

  <div id="panbar"><div id="panthumb"></div></div>

  <div id="legend"></div>

  <div class="controls">
    <span id="tlabel" class="mono">00:00 / 00:00</span>
    <span class="spacer"></span>
    <div id="zoom">
      <button id="zoomout" type="button" aria-label="Уменьшить масштаб">−</button>
      <button id="zoomin" type="button" aria-label="Увеличить масштаб">+</button>
    </div>
    <button id="zoomreset" type="button" aria-label="Сбросить масштаб">⤢</button>
    <div id="mode">
      <button data-m="mag" class="mbtn on">|a|</button>
      <button data-m="xyz" class="mbtn">X / Y / Z</button>
    </div>
  </div>
  <div id="warnings"></div>

  <canvas id="scrub2" height="240"></canvas>

  <div id="legend2"></div>

  <div class="controls">
    <span class="glabel">Гироскоп</span>
    <span class="spacer"></span>
    <div id="gmode">
      <button data-m="mag" class="gbtn on">|ω|</button>
      <button data-m="xyz" class="gbtn">X / Y / Z</button>
    </div>
  </div>

  <canvas id="scrub3" height="240"></canvas>
  <div id="legend3"></div>
  <div class="controls">
    <span class="glabel">Гравитация</span>
    <span class="spacer"></span>
    <div id="rmode">
      <button data-m="mag" class="rbtn">|g|</button>
      <button data-m="xyz" class="rbtn on">X / Y / Z</button>
    </div>
  </div>

  <canvas id="scrub4" height="240"></canvas>
  <div id="legend4"></div>
  <div class="controls"><span class="glabel">Ориентация камеры (кватернион)</span></div>
  </div>
```

- [ ] **Step 3: App-shell layout in `style.css`**

In `gopro_accel/static/style.css`, replace:

```css
body { font-family: system-ui, sans-serif; margin: 16px; max-width: 900px; }
```

with:

```css
html, body { height: 100%; }
body { font-family: system-ui, sans-serif; margin: 16px; max-width: 900px;
  display: flex; flex-direction: column; height: calc(100vh - 32px); }
.bar, #stage { flex: 0 0 auto; }
#graphs { flex: 1 1 auto; overflow-y: auto; min-height: 0; }
```

- [ ] **Step 4: Manual check**

Run `python -m gopro_accel --root "<folder with mp4s>"`, open a clip: four graphs (no image-orientation); the video and file bar stay fixed while the graphs area scrolls; a `Копировать путь` button sits in the file bar. (Behaviour for the button and dropdown comes in Task 3.)

- [ ] **Step 5: Commit**

```bash
git add gopro_accel/static/index.html gopro_accel/static/style.css
git commit -m "feat: fixed video with scrollable graphs; copy-path button; remove IORI markup"
```

---

## Task 3: Frontend behaviour — remove IORI graph, directory dropdown, copy handler

**Files:**
- Modify: `gopro_accel/static/app.js`

- [ ] **Step 1: Remove the IORI entry from `GRAPHS`**

In `gopro_accel/static/app.js`, delete this line from the `GRAPHS` array:

```javascript
  { canvasId: "scrub5", legendId: "legend5", tKey: "iot", unit: "", fixed: quat("iow", "iox", "ioy", "ioz") },
```

- [ ] **Step 2: Populate the dropdown from the opened file's directory**

Replace the Обзор file-row click handler — this block inside `browse`:

```javascript
    else fslist.appendChild(makeRow("🎬", e.name, () => {
      modal.classList.add("hidden");
      addFileOption(e.path, e.name);
      openFile(e.path);
    }));
```

with:

```javascript
    else fslist.appendChild(makeRow("🎬", e.name, () => {
      modal.classList.add("hidden");
      rootDir = body.cwd;
      populateList(body.cwd, e.path);
    }));
```

Then remove the now-unused `addFileOption` function:

```javascript
function addFileOption(path, name) {
  const sel = document.getElementById("filesel");
  const opt = document.createElement("option");
  opt.value = path; opt.textContent = name; opt.selected = true;
  sel.insertBefore(opt, sel.firstChild);
}
```

And replace `populateList` to accept an optional `selectPath`:

```javascript
async function populateList(root) {
  const res = await fetch(`/api/browse?path=${encodeURIComponent(root)}`);
  const body = await res.json();
  const sel = document.getElementById("filesel");
  sel.innerHTML = "";
  for (const e of (body.entries || []).filter((x) => !x.is_dir)) {
    const opt = document.createElement("option");
    opt.value = e.path; opt.textContent = e.name;
    sel.appendChild(opt);
  }
  if (sel.value) openFile(sel.value);
}
```

with:

```javascript
async function populateList(root, selectPath) {
  const res = await fetch(`/api/browse?path=${encodeURIComponent(root)}`);
  const body = await res.json();
  const sel = document.getElementById("filesel");
  sel.innerHTML = "";
  for (const e of (body.entries || []).filter((x) => !x.is_dir)) {
    const opt = document.createElement("option");
    opt.value = e.path; opt.textContent = e.name;
    sel.appendChild(opt);
  }
  if (selectPath) {
    sel.value = selectPath;
    openFile(selectPath);
  } else if (sel.value) {
    openFile(sel.value);
  }
}
```

- [ ] **Step 3: Wire the copy-path button**

Immediately AFTER the `document.getElementById("browse").addEventListener("click", ...)` handler, add:

```javascript
document.getElementById("copypath").addEventListener("click", async () => {
  if (!curPath) return;
  try {
    await navigator.clipboard.writeText(curPath);
  } catch (err) { /* clipboard blocked — still show feedback */ }
  const btn = document.getElementById("copypath");
  const original = btn.textContent;
  btn.textContent = "Скопировано";
  setTimeout(() => { btn.textContent = original; }, 1200);
});
```

- [ ] **Step 4: Verify it parses**

Run `node --check gopro_accel/static/app.js` if node exists; otherwise re-read the whole file confirming brace/paren balance, that `scrub5`/`iot`/`addFileOption` no longer appear, and that `GRAPHS` now has 4 entries. Report the method and grep result.

- [ ] **Step 5: Manual acceptance**

Run `python -m gopro_accel --root "<folder with mp4s>"`. Confirm:
- Four graphs only (no image orientation); everything else (zoom, pan, toggles, arrow keys) still works.
- The video stays fixed while the graphs area scrolls.
- Opening a file via Обзор fills the dropdown with that folder's `.MP4` files; switching the dropdown loads other files from the folder.
- The `Копировать путь` button copies the current file's full path (paste to verify) and briefly reads `Скопировано`; it does nothing before a file is opened.

- [ ] **Step 6: Commit**

```bash
git add gopro_accel/static/app.js
git commit -m "feat: dropdown lists the opened file's directory; copy-path button; drop IORI graph"
```

---

## Self-review

**Spec coverage:** remove IORI → Task 1 (backend), Task 2 Step 2 (markup), Task 3 Step 1 (GRAPHS); fixed video + scrollable graphs → Task 2 Steps 2–3 (`#graphs` wrapper + flex layout); dropdown from directory → Task 3 Step 2 (`populateList(root, selectPath)` + browse handler); copy button → Task 2 Step 1 (markup) + Task 3 Step 3 (handler).

**Placeholder scan:** none — full code for every step.

**Type/name consistency:** `populateList(root, selectPath)` called with `(body.cwd, e.path)` from the browse handler and with a single arg from `usefolder`; `#copypath` created in Task 2 Step 1 and wired in Task 3 Step 3; `#graphs` created in Task 2 Step 2 and styled in Task 2 Step 3; `addFileOption` removed with its only caller; the removed `scrub5`/IORI has no remaining references (Task 3 Step 4 grep).
